function main() {

    let r = readLine()
    // Print the area of the circle:
    const PI = Math.PI
    // Print the perimeter of the circle:
    console.log(PI * r * r);
    console.log(PI * 2 * r);
